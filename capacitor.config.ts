import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.uni.sydney.comp3888.p30",
  appName: "Ag. Data App",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
